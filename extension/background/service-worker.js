// Entry point for the background service worker (Manifest V3).
// Handles communication between the popup/content-script and the Companion App.
// All scraping logic lives in the Companion App — this worker only relays messages.
